# temp

You are a Senior Remote Sensing Scientist, Spatial Statistician, GIS Engineer, and Python Software Architect.

You are contributing to the Boreas-Nexus project.

Module 1 (Urban Heat Hotspot Identification Engine) is already fully implemented.

The following capabilities already exist:

✓ Day and Night LST
✓ SUHII computation
✓ Urban and Rural Masks
✓ Heat Persistence Index
✓ Getis–Ord Gi* hotspot analysis
✓ Urban Heat Hotspot Knowledge Layer
✓ GeoJSON output for every spatial point

DO NOT rewrite any existing code.

Your task is ONLY to extend Module 1 with three additional capabilities while preserving the existing architecture and maintaining backward compatibility.

# ======================================================================
OBJECTIVES

Implement

1. Hotspot Cluster Metadata
2. Hotspot Confidence Score
3. City Temperature Percentile

The implementation must integrate seamlessly into the existing Module 1 pipeline.

The Urban Heat Hotspot Knowledge Layer must remain the single source of truth.

Do not create separate knowledge datasets containing duplicate information.

# ======================================================================
ARCHITECTURAL REQUIREMENTS

The Urban Heat Hotspot Knowledge Layer remains the authoritative dataset consumed by every downstream module.

The project follows an Analytics-First storage architecture.

GeoParquet shall be the primary storage format for all internal geospatial knowledge.

Parquet shall be the primary storage format for all relational datasets.

GeoJSON and GeoPackage are export formats only.

No internal module should depend on GeoJSON for reading or writing data.

The new information must be integrated directly into the existing Urban Heat Hotspot Knowledge Layer.

The pipeline should produce

Primary Internal Outputs

Knowledge Layer

urban_heat_hotspot_knowledge_layer.geoparquet

Hotspot Registry

hotspot_registry.parquet

Validation Reports

cluster_validation.json

metadata.json

Optional Export Products

urban_heat_hotspots.geojson

urban_heat_hotspots.gpkg

hotspot_clusters.geojson

hotspot_clusters.gpkg

GeoJSON and GeoPackage are generated only for

- GIS interoperability
- visualization
- web mapping
- external sharing

The knowledge layer stored as GeoParquet is the single source of truth.

All future Boreas-Nexus modules must read from the GeoParquet knowledge layer.

# ======================================================================
PART 1
HOTSPOT CLUSTER METADATA

Current Situation

Each point knows whether it is a hotspot.

However, hotspot regions are not identified.

Implement a Hotspot Cluster Generator.

Workflow

Getis–Ord Gi*

↓

Threshold Significant Pixels

↓

Binary Hotspot Raster

↓

Connected Component Analysis

↓

Hotspot Regions

Every connected hotspot region should receive a unique identifier.

Example

HOT_0001

HOT_0002

HOT_0003

For every hotspot compute

hotspot_id

cluster_area_m2

cluster_perimeter_m

cluster_size_pixels

cluster_centroid

cluster_bbox

# ======================================================================
KNOWLEDGE LAYER INTEGRATION

The existing Urban Heat Hotspot Knowledge Layer must be enriched.

The authoritative dataset is

urban_heat_hotspots.geoparquet

Each point should include

hotspot_id

city_temperature_percentile

temperature_rank

hotspot_confidence_score

confidence_class

Only hotspot_id should be duplicated across hotspot members.

Cluster-level metadata must NOT be duplicated for every point.

Instead implement a normalized Hotspot Registry.

Store

hotspot_registry.parquet

Each hotspot record should contain

hotspot_id

cluster_area_m2

cluster_perimeter_m

cluster_size_pixels

cluster_centroid

cluster_bbox

mean_lst

peak_lst

mean_suhii

mean_heat_persistence

mean_hotspot_confidence_score

The Urban Heat Hotspot Knowledge Layer references hotspots using hotspot_id.

If a point does not belong to a hotspot

hotspot_id = null

This design minimizes redundancy while maintaining efficient joins.

# ======================================================================
IMPLEMENTATION REQUIREMENTS

Use

scipy.ndimage.label

or an equivalent connected component algorithm.

Support

4-neighbour connectivity

8-neighbour connectivity

Connectivity must be configurable.

Generate hotspot polygons from connected components.

Export

hotspot_clusters.geojson

hotspot_clusters.gpkg

These outputs are derived visualization layers only.

# ======================================================================
PART 2
HOTSPOT CONFIDENCE SCORE

Replace the current binary hotspot interpretation with an additional deterministic confidence score.

The confidence score should range

0

↓

100

The implementation must NOT use machine learning.

The confidence score should combine

Gi* Z-score

Surface Urban Heat Island Intensity (SUHII)

Heat Persistence Index

City Temperature Percentile

Implement a configurable weighted scoring model.

Default weights

Gi* Z-score

45%

SUHII

30%

Heat Persistence

15%

City Temperature Percentile

10%

Normalize every component before weighting.

Store

hotspot_confidence_score

confidence_class

Suggested confidence classes

Very Low

Low

Moderate

High

Very High

Critical

Configuration must be stored in

config/hotspot_scoring.yaml

Never hardcode weights.

# ======================================================================
PART 3
CITY TEMPERATURE PERCENTILE

For every point compute

city_temperature_percentile

based on

Day LST

The percentile represents the relative thermal ranking within the study area.

Example

98.4

means

Hotter than 98.4% of all valid land pixels within the city.

Requirements

Ignore NoData

Ignore Water

Support configurable masking

Use scipy or pandas ranking methods.

Store

city_temperature_percentile

temperature_rank

temperature_total_pixels

# ======================================================================
PIPELINE INTEGRATION

Current Pipeline

Gi*

↓

Knowledge Layer

Extended Pipeline

Gi*

↓

Cluster Generator

↓

City Temperature Percentile

↓

Hotspot Confidence Score

↓

Knowledge Layer Builder

↓

Derived Products

The Knowledge Layer Builder must enrich the existing point features.

Each point should contain

hotspot_id

city_temperature_percentile

temperature_rank

hotspot_confidence_score

confidence_class

No additional point-based GeoJSONs should be created.

# ======================================================================
VALIDATION

Implement validation checks.

Cluster IDs must be unique.

Every hotspot point must belong to exactly one hotspot.

Confidence scores

0–100

Percentiles

0–100

Cluster area

> 0
> 

Cluster perimeter

> 0
> 

Generate

cluster_validation.json

# ======================================================================
OUTPUT FILES

PRIMARY INTERNAL DATASETS

Knowledge Layer

urban_heat_hotspots.geoparquet

This is the authoritative geospatial knowledge dataset.

Every downstream Boreas-Nexus module must consume this dataset.

Hotspot Registry

hotspot_registry.parquet

One record per hotspot.

Contains cluster-level metadata.

No duplicated point-level information.

VALIDATION

cluster_validation.json

metadata.json

OPTIONAL EXPORTS

GeoJSON

urban_heat_hotspots.geojson

GeoPackage

urban_heat_hotspots.gpkg

Hotspot Polygons

hotspot_clusters.geojson

hotspot_clusters.gpkg

These are derived visualization products only.

They must never become the primary internal storage format.

# ======================================================================
STORAGE OPTIMIZATION REQUIREMENTS

The implementation should optimize for large-scale geospatial analytics.

Requirements

Use GeoParquet for internal spatial datasets.

Use Parquet for relational datasets.

Avoid storing duplicate hotspot metadata for every point.

Normalize hotspot metadata into the Hotspot Registry.

Preserve geometry using GeoParquet.

Use efficient Arrow-compatible data types.

Avoid unnecessary GeoJSON serialization during pipeline execution.

Generate GeoJSON only when explicitly exporting.

Design the storage layer to support future processing of millions of spatial points across multiple cities.

Future modules should be able to filter, join, and query datasets directly from Parquet/GeoParquet without format conversion.

# ======================================================================
CODING STANDARDS

Do NOT rewrite existing stages.

Only extend them.

Use

Python 3.12+

Type hints

PEP8

SOLID

Pathlib

Structured logging

Dependency Injection where appropriate

Enterprise-quality code.

# ======================================================================
OUTPUT REQUIREMENTS

Do not regenerate the entire module.

First inspect the existing Module 1 architecture.

Identify exactly where these additions should integrate.

Explain the modifications.

Generate only the new classes and the minimal changes required.

Preserve backward compatibility.

Do not duplicate existing functionality.

The resulting architecture should keep the Urban Heat Hotspot Knowledge Layer as the authoritative dataset while generating derived hotspot products for visualization, GIS interoperability, and reporting.